<h1><%=@patient.name%></h1>

<%= pluralize(@patient.appointments.count, 'Appointment') + ':' %>
<ul>
    <% @patient.appointments.each do |a| %>
        <li><%= a.readable %> with <%= link_to a.doctor.name, doctor_path(a.doctor) %></li>
    <% end %>